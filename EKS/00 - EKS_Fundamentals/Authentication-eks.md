# Authentication

- Components (`OIDC, Auth ConfigMap, EKS Auth`) that work together to figure out the user and if the user can have access to reach the API server
- Also authenticates if a pod can call s3 bucket.
- For a pod to have access s3 bucket, 

### AWS IAM Authenticator


### OIDC



### Auth ConfigMap


### EKS Auth
