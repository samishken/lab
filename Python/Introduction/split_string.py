# String Manipulation: Gain familiarity with basic string operations in Python,
# particularly the split() method,
# which divides a string into a list of substrings based on a specified delimiter

text = "Hello World"

splitted_text = text.split()
print(splitted_text)